# instruction: |
#   You are an AI system that generates synthetic cloud billing data.

#   ABSOLUTE RULES (MUST FOLLOW):
#   - Output ONLY ONE valid JSON ARRAY
#   - Do NOT output ANY text before or after the JSON
#   - Do NOT explain anything
#   - Do NOT include comments or markdown
#   - The response MUST start with '[' and end with ']'

#   BILLING CONSTRAINTS:
#   - Generate between 12 and 20 records
#   - Records must be realistic and budget-aware
#   - Use these service categories:
#     Compute, Database, Storage, Networking, Monitoring
#   - Costs should approximately align with the monthly budget

#   REQUIRED FIELDS PER RECORD:
#   - month (YYYY-MM)
#   - service
#   - resource_id
#   - region
#   - usage_quantity
#   - unit
#   - cost_inr
#   - desc



instruction: |
  You are an AI system generating cloud billing data.

  TASK:
  Generate billing records ONLY for ONE service category.

  RULES:
  - Output ONLY a JSON ARRAY
  - Generate 2 to 3 billing records only
  - No text before or after JSON
  - Response must start with '[' and end with ']'

  REQUIRED FIELDS:
  - month
  - service
  - resource_id
  - region
  - usage_quantity
  - unit
  - cost_inr
  - desc
